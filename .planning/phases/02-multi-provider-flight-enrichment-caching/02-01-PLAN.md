---
phase: "02"
plan: "01"
title: "Build Flightradar24 provider, adsbdb.com fallback, CSV type resolver, and multi-provider orchestrator"
wave: 1
depends_on: []
files_modified:
  - src/services/enrichment/flightradar.js
  - src/services/enrichment/adsbdb.js
  - src/services/enrichment/csv-aircraft.js
  - src/services/enrichment/index.js
autonomous: true
requirements:
  - ENRICH-01
  - ENRICH-02
  - ENRICH-04
must_haves:
  truths:
    - "Flightradar24 client extracts airline, origin, destination, and aircraft model by flight callsign/registration"
    - "adsbdb.com provider acts as secondary fallback when Flightradar24 fails or returns incomplete data"
    - "Local CSV resolver loads aircraft types and maps ICAO codes to friendly descriptions"
    - "Enrichment orchestrator tries Flightradar24 -> adsbdb.com -> CSV -> fallback payload without unhandled crashes"
  artifacts:
    - path: "src/services/enrichment/flightradar.js"
      provides: "Primary Flightradar24 enrichment provider"
      exports: ["fetchFlightradarData"]
    - path: "src/services/enrichment/adsbdb.js"
      provides: "Secondary adsbdb.com enrichment provider"
      exports: ["fetchAdsbdbData"]
    - path: "src/services/enrichment/csv-aircraft.js"
      provides: "Local ICAO aircraft types CSV parser and indexer"
      exports: ["loadAircraftTypes", "lookupAircraftType"]
    - path: "src/services/enrichment/index.js"
      provides: "Multi-provider flight enrichment orchestrator"
      exports: ["enrichFlight", "createEnricher"]
  key_links:
    - from: "src/services/enrichment/index.js"
      to: "src/services/enrichment/flightradar.js"
      via: "queries primary Flightradar24 provider"
      pattern: "fetchFlightradarData"
    - from: "src/services/enrichment/index.js"
      to: "src/services/enrichment/adsbdb.js"
      via: "queries secondary adsbdb.com provider on fallback"
      pattern: "fetchAdsbdbData"
    - from: "src/services/enrichment/index.js"
      to: "src/services/enrichment/csv-aircraft.js"
      via: "resolves aircraft model name from ICAO code"
      pattern: "lookupAircraftType"
---

<objective>
Implement the multi-provider flight data enrichment pipeline with Flightradar24 as primary, adsbdb.com as secondary fallback, and local CSV ICAO aircraft type mapping.

Purpose: Deliver accurate, rich flight route and aircraft information for local overhead flights while eliminating flakiness and unhandled crashes.
Output: src/services/enrichment/flightradar.js, src/services/enrichment/adsbdb.js, src/services/enrichment/csv-aircraft.js, src/services/enrichment/index.js.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architecture-multi-source-configuration/01-01-SUMMARY.md
@src/config/index.js
@src/utils/logger.js
@flightscanner.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Flightradar24 provider</name>
  <files>src/services/enrichment/flightradar.js</files>
  <read_first>flightscanner.js, .planning/research/FEATURES.md</read_first>
  <action>
    Create `src/services/enrichment/flightradar.js`:
    - Implement `fetchFlightradarData({ callsign, registration, hex })` using `flightradarapi` or HTTP client with 5s timeout.
    - Query Flightradar24 for flight details given flight callsign or registration.
    - Normalize returned data into standard flight schema:
      - `callsign`: string (e.g. "DLH123")
      - `airline`: string (e.g. "Lufthansa")
      - `origin`: string (e.g. "MUC" / "Munich")
      - `destination`: string (e.g. "HAM" / "Hamburg")
      - `aircraft`: string (e.g. "Airbus A320-214")
      - `aircraftIcao`: string (e.g. "A320")
      - `source`: "flightradar24"
    - Return `null` if flight not found or on network/rate-limit error, with debug logging.
  </action>
  <verify>node -e 'const { fetchFlightradarData } = require("./src/services/enrichment/flightradar"); console.log("Flightradar provider module loaded successfully");'</verify>
  <done>src/services/enrichment/flightradar.js exports fetchFlightradarData</done>
</task>

<task type="auto">
  <name>Task 2: Implement adsbdb.com provider and CSV aircraft types loader</name>
  <files>src/services/enrichment/adsbdb.js, src/services/enrichment/csv-aircraft.js</files>
  <read_first>flightscanner.js</read_first>
  <action>
    1. Create `src/services/enrichment/adsbdb.js`:
       - Implement `fetchAdsbdbData({ callsign, registration })` using `axios` with 4s timeout.
       - Query `https://api.adsbdb.com/v0/aircraft/${registration}?callsign=${callsign}`.
       - Normalize response into standard flight schema with `source: "adsbdb"`.
       - Catch and handle 404s, timeouts, and network errors gracefully, returning `null`.
    2. Create `src/services/enrichment/csv-aircraft.js`:
       - Implement `loadAircraftTypes(csvPath)` using `papaparse` and `fs.createReadStream` to index ICAO codes.
       - Implement `lookupAircraftType(icaoCode)` to return friendly name/model (e.g. "A320" -> "Airbus A320").
       - Handle missing CSV file gracefully without throwing errors (log warning and return empty map).
  </action>
  <verify>node -e 'const { fetchAdsbdbData } = require("./src/services/enrichment/adsbdb"); const { loadAircraftTypes, lookupAircraftType } = require("./src/services/enrichment/csv-aircraft"); console.log("adsbdb and CSV modules loaded successfully");'</verify>
  <done>src/services/enrichment/adsbdb.js and src/services/enrichment/csv-aircraft.js export required methods</done>
</task>

<task type="auto">
  <name>Task 3: Implement multi-provider enrichment orchestrator</name>
  <files>src/services/enrichment/index.js</files>
  <read_first>src/services/enrichment/flightradar.js, src/services/enrichment/adsbdb.js, src/services/enrichment/csv-aircraft.js</read_first>
  <action>
    Create `src/services/enrichment/index.js`:
    - Implement `createEnricher({ config, cache })` returning an enricher instance with `enrichFlight(telemetry)`.
    - Pipeline logic:
      1. Check cache (if cache provided) for callsign or registration. If hit, return cached flight.
      2. Attempt primary `fetchFlightradarData`.
      3. If Flightradar fails or lacks origin/destination, attempt secondary `fetchAdsbdbData`.
      4. If aircraft model is missing or generic, resolve via `lookupAircraftType(telemetry.t || telemetry.aircraftIcao)`.
      5. Construct finalized enriched flight object:
         - `airline`: airline or "Unbekannte Fluggesellschaft"
         - `callsign`: trimmed callsign or "Unbekannt"
         - `origin`: origin or "Unbekannt"
         - `destination`: destination or "Unbekannt"
         - `aircraft`: resolved aircraft model or "Unbekannt"
         - `registration`: registration or hex
         - `source`: "flightradar24" | "adsbdb" | "csv" | "fallback"
      6. Cache successful result in route cache.
  </action>
  <verify>node -e 'const { createEnricher } = require("./src/services/enrichment"); console.log("Enrichment orchestrator loaded successfully");'</verify>
  <done>src/services/enrichment/index.js properly orchestrates fallback chain</done>
</task>
</tasks>

<verification>
- [ ] src/services/enrichment/flightradar.js handles Flightradar24 queries with timeout
- [ ] src/services/enrichment/adsbdb.js queries adsbdb.com fallback with timeout
- [ ] src/services/enrichment/csv-aircraft.js parses and indexes ICAO aircraft types
- [ ] src/services/enrichment/index.js orchestrates fallback chain without crashing
</verification>

<success_criteria>
- All tasks completed
- Multi-provider fallback chain (Flightradar24 -> adsbdb.com -> CSV -> fallback) operates reliably
- Network and parsing errors are isolated and never crash the process
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-provider-flight-enrichment-caching/02-01-SUMMARY.md`
</output>
