---
phase: "03"
plan: "02"
title: "Implement Unit Test Suite for Tracking, Geofencing, Queueing, Formatter, and MQTT Publisher"
wave: 2
depends_on:
  - "03-01"
files_modified:
  - test/geofence.test.js
  - test/queue.test.js
  - test/formatter.test.js
  - test/mqtt.test.js
autonomous: true
requirements:
  - TRACK-01
  - TRACK-02
  - TRACK-03
  - TRACK-04
  - NOTIFY-01
  - NOTIFY-02
  - NOTIFY-03
must_haves:
  truths:
    - "Unit tests verify geofence coordinate and altitude boundary checks"
    - "Unit tests verify multi-aircraft queue sequencing and cooldown suppression"
    - "Unit tests verify German notification text variations and icon selection logic"
    - "Unit tests verify MQTT client options, payload serialization, and mock publishing"
  artifacts:
    - path: "test/geofence.test.js"
      provides: "Unit tests for geofence and altitude filtering"
      min_lines: 30
    - path: "test/queue.test.js"
      provides: "Unit tests for queue sequencing, debounce, and cooldowns"
      min_lines: 40
    - path: "test/formatter.test.js"
      provides: "Unit tests for German notification formatting and icon mappings"
      min_lines: 30
    - path: "test/mqtt.test.js"
      provides: "Unit tests for MQTT publisher options and payload dispatch"
      min_lines: 30
  key_links:
    - from: "test/geofence.test.js"
      to: "src/services/tracking/geofence.js"
      via: "tests isAircraftInGeofence"
      pattern: "isAircraftInGeofence"
    - from: "test/queue.test.js"
      to: "src/services/tracking/queue.js"
      via: "tests FlightQueue enqueue and cooldown logic"
      pattern: "FlightQueue"
    - from: "test/formatter.test.js"
      to: "src/services/notification/formatter.js"
      via: "tests formatNotificationPayload and getAirlineIcon"
      pattern: "formatNotificationPayload"
---

<objective>
Author comprehensive automated unit test suites for geofence filtering, multi-aircraft queueing and cooldowns, notification formatting, and MQTT publishing.

Purpose: Guarantee 100% test coverage and prevent regressions across all tracking and notification features.
Output: test/geofence.test.js, test/queue.test.js, test/formatter.test.js, test/mqtt.test.js.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-telemetry-watcher-multi-aircraft-queue-awtrix-mqtt-notifier/03-01-PLAN.md
@src/services/tracking/geofence.js
@src/services/tracking/queue.js
@src/services/notification/formatter.js
@src/services/notification/mqtt.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement geofence and queue unit tests</name>
  <files>test/geofence.test.js, test/queue.test.js</files>
  <read_first>src/services/tracking/geofence.js, src/services/tracking/queue.js</read_first>
  <action>
    1. Create `test/geofence.test.js`:
       - Test valid aircraft inside latitude/longitude and altitude range returns `true`.
       - Test aircraft outside latitude bounds returns `false`.
       - Test aircraft outside longitude bounds returns `false`.
       - Test aircraft below min altitude or above max altitude returns `false`.
       - Test missing/invalid coordinates or altitude returns `false`.
    2. Create `test/queue.test.js`:
       - Test enqueuing items calls `processCallback` sequentially.
       - Test identical flight within cooldown window is suppressed and not re-enqueued.
       - Test multiple distinct flights are processed in order.
       - Test cooldown expiration allows flight to be enqueued again.
  </action>
  <verify>node --test test/geofence.test.js test/queue.test.js</verify>
  <done>Geofence and Queue unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Implement formatter and MQTT unit tests</name>
  <files>test/formatter.test.js, test/mqtt.test.js</files>
  <read_first>src/services/notification/formatter.js, src/services/notification/mqtt.js</read_first>
  <action>
    1. Create `test/formatter.test.js`:
       - Test German text formatting when origin, destination, airline, callsign, and aircraft are provided.
       - Test German text formatting when origin/destination are unknown.
       - Test airline icon selection matching callsign prefix (e.g. "DLH", "EWG", "KLM").
       - Test default icon fallback (1391) when airline/callsign is unknown.
       - Test payload structure conforms to Awtrix specifications (`{ icon, repeat, scroll: { speed }, text }`).
    2. Create `test/mqtt.test.js`:
       - Test MQTT publisher initialization options.
       - Test publishing serialized notification payload to configured topic.
       - Test clean closure.
  </action>
  <verify>npm test</verify>
  <done>All test suites pass via npm test</done>
</task>
</tasks>

<verification>
- [ ] test/geofence.test.js verifies bounding box and altitude filtering
- [ ] test/queue.test.js verifies queue ordering and cooldown mechanics
- [ ] test/formatter.test.js verifies German text and icon matching
- [ ] test/mqtt.test.js verifies MQTT publishing logic
- [ ] npm test runs full test suite cleanly
</verification>

<success_criteria>
- All tasks completed
- Full automated test suite passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-telemetry-watcher-multi-aircraft-queue-awtrix-mqtt-notifier/03-02-SUMMARY.md`
</output>
