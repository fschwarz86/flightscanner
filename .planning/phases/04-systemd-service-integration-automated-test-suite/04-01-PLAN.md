---
phase: "04"
plan: "01"
title: "Implement Main Application Entry Point, Graceful Shutdown, and Systemd Service Unit"
wave: 1
depends_on: []
files_modified:
  - src/index.js
  - systemd/flightscanner.service
  - config/default-flightscanner.example
  - config/flightscanner.json.example
autonomous: true
requirements:
  - CONF-02
must_haves:
  truths:
    - "Application entry point wires together config, enrichment, queue, watcher, and MQTT publisher"
    - "Graceful shutdown hook cleans up watcher, MQTT connection, and timer resources on SIGINT and SIGTERM"
    - "Systemd service unit provides 24/7 headless operation with automatic restart and journalctl output"
    - "Example systemd environment and JSON config files provide easy deployment templates"
  artifacts:
    - path: "src/index.js"
      provides: "Main flightscanner application daemon"
      exports: ["main", "startApp"]
    - path: "systemd/flightscanner.service"
      provides: "Systemd service definition"
    - path: "config/default-flightscanner.example"
      provides: "Example /etc/default/flightscanner environment file"
    - path: "config/flightscanner.json.example"
      provides: "Example /etc/flightscanner/config.json configuration file"
  key_links:
    - from: "src/index.js"
      to: "src/config/index.js"
      via: "loads and validates runtime config"
      pattern: "loadConfig"
    - from: "src/index.js"
      to: "src/services/enrichment/index.js"
      via: "initializes enrichment pipeline"
      pattern: "createEnricher"
    - from: "src/index.js"
      to: "src/services/tracking/watcher.js"
      via: "starts file telemetry watcher"
      pattern: "createTelemetryWatcher"
    - from: "src/index.js"
      to: "src/services/notification/mqtt.js"
      via: "connects and publishes to MQTT broker"
      pattern: "createMqttPublisher"
---

<objective>
Implement the main application entry point (src/index.js), graceful process termination handlers, and production systemd service unit files.

Purpose: Deliver a turn-key, headless 24/7 Linux daemon service that starts reliably, recovers from faults, and terminates cleanly.
Output: src/index.js, systemd/flightscanner.service, config/default-flightscanner.example, config/flightscanner.json.example.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/config/index.js
@src/utils/logger.js
@src/services/enrichment/index.js
@src/services/enrichment/cache.js
@src/services/tracking/queue.js
@src/services/tracking/watcher.js
@src/services/notification/mqtt.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement main application entry point and graceful shutdown</name>
  <files>src/index.js</files>
  <read_first>src/config/index.js, src/utils/logger.js, src/services/enrichment/index.js, src/services/tracking/queue.js, src/services/tracking/watcher.js, src/services/notification/mqtt.js</read_first>
  <action>
    Create `src/index.js`:
    - Load configuration with `loadConfig()` and validate with `validateConfig(config)`.
    - Initialize `FlightCache`, `createEnricher`, `createMqttPublisher`, `FlightQueue`, and `createTelemetryWatcher`.
    - Connect MQTT publisher and start telemetry watcher.
    - Wire telemetry watcher -> queue.enqueue -> enricher.enrichFlight -> mqtt.publishNotification.
    - Implement `startApp(options)` returning `{ stop }` for programmatic testability.
    - Attach `SIGINT`, `SIGTERM`, `uncaughtException`, and `unhandledRejection` hooks to log and cleanly stop services without hanging.
  </action>
  <verify>node -e 'const { startApp } = require("./src/index"); console.log("src/index.js loaded successfully");</verify>
  <done>src/index.js exports startApp and boots all subsystems</done>
</task>

<task type="auto">
  <name>Task 2: Create systemd unit file and example config files</name>
  <files>systemd/flightscanner.service, config/default-flightscanner.example, config/flightscanner.json.example</files>
  <read_first>.env.example</read_first>
  <action>
    1. Create `systemd/flightscanner.service`:
       - Define `[Unit]` with `Description=Flightscanner ADS-B Matrix Display Service`, `After=network.target dump1090-fa.service mosquitto.service`.
       - Define `[Service]` with `Type=simple`, `User=pi` (or configurable), `WorkingDirectory=/opt/flightscanner`, `EnvironmentFile=-/etc/default/flightscanner`, `ExecStart=/usr/bin/node /opt/flightscanner/src/index.js`, `Restart=always`, `RestartSec=5s`, `StandardOutput=journal`, `StandardError=journal`.
       - Define `[Install]` with `WantedBy=multi-user.target`.
    2. Create `config/default-flightscanner.example` with sample environment configuration.
    3. Create `config/flightscanner.json.example` with sample JSON configuration.
  </action>
  <verify>test -f systemd/flightscanner.service && test -f config/default-flightscanner.example && test -f config/flightscanner.json.example</verify>
  <done>Systemd service and example configs created</done>
</task>
</tasks>

<verification>
- [ ] src/index.js wires all subsystems together and exports startApp
- [ ] systemd/flightscanner.service contains Restart=always, StandardOutput=journal, and EnvironmentFile
- [ ] config/ default examples provide clear configuration reference
</verification>

<success_criteria>
- All tasks completed
- Clean application startup and graceful shutdown lifecycle
- Production-ready systemd configuration
</success_criteria>

<output>
After completion, create `.planning/phases/04-systemd-service-integration-automated-test-suite/04-01-SUMMARY.md`
</output>
