---
phase: "04"
plan: "02"
title: "Implement End-to-End Integration Test Suite and Full Verification"
wave: 2
depends_on:
  - "04-01"
files_modified:
  - test/integration.test.js
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "End-to-end integration test exercises full pipeline from mock aircraft.json write to MQTT publish"
    - "Graceful shutdown is verified during test execution"
    - "All unit and integration tests pass with npm test"
  artifacts:
    - path: "test/integration.test.js"
      provides: "End-to-end pipeline integration test"
      min_lines: 50
  key_links:
    - from: "test/integration.test.js"
      to: "src/index.js"
      via: "starts and exercises startApp with mock telemetry file and broker"
      pattern: "startApp"
---

<objective>
Implement the end-to-end integration test suite and perform final verification across all requirements.

Purpose: Validate that the complete refactored system works seamlessly as a cohesive application.
Output: test/integration.test.js.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-systemd-service-integration-automated-test-suite/04-01-PLAN.md
@src/index.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement End-to-End Integration Test</name>
  <files>test/integration.test.js</files>
  <read_first>src/index.js</read_first>
  <action>
    Create `test/integration.test.js` using `node:test` and `node:assert/strict`:
    - Setup temporary directory with mock `aircraft.json` and `aircraft_types.csv`.
    - Initialize `startApp` with mock MQTT client and mock Flightradar API provider.
    - Write mock aircraft data inside geofence into `aircraft.json`.
    - Verify:
      1. Watcher detects file write and parses aircraft telemetry.
      2. Geofence validates aircraft coordinates and altitude.
      3. Queue enqueues aircraft and triggers enrichment.
      4. Enricher resolves flight route and aircraft model.
      5. Formatter constructs German text and selects airline icon.
      6. MQTT publisher receives formatted notification payload.
      7. Subsequent immediate write of same flight is suppressed by cooldown.
      8. `app.stop()` cleanly terminates all watchers and timers.
  </action>
  <verify>npm test</verify>
  <done>Integration test and entire test suite execute and pass cleanly</done>
</task>
</tasks>

<verification>
- [ ] test/integration.test.js verifies complete end-to-end pipeline
- [ ] npm test runs all unit and integration test suites with 100% pass rate
</verification>

<success_criteria>
- All tasks completed
- Full test suite passes
- End-to-end flight detection, enrichment, and notification workflow validated
</success_criteria>

<output>
After completion, create `.planning/phases/04-systemd-service-integration-automated-test-suite/04-02-SUMMARY.md`
</output>
