---
phase: "01"
plan: "02"
title: "Unit tests for hierarchical configuration loader, validation, and logger"
wave: 2
depends_on:
  - "01-01"
files_modified:
  - test/config.test.js
  - test/logger.test.js
autonomous: true
requirements:
  - CONF-01
  - CONF-03
must_haves:
  truths:
    - "npm test runs automated test suite using Node.js built-in test runner"
    - "Config loader tests verify fallback priority: /etc/flightscanner/config.json -> .env -> defaults"
    - "Config validator tests verify bounds validation and rejection of invalid values"
  artifacts:
    - path: "test/config.test.js"
      provides: "Unit tests for configuration loading, hierarchy, and validation"
      min_lines: 40
    - path: "test/logger.test.js"
      provides: "Unit tests for log level filtering and output formatting"
      min_lines: 20
  key_links:
    - from: "test/config.test.js"
      to: "src/config/index.js"
      via: "imports loadConfig and validateConfig"
      pattern: "require.*src/config"
    - from: "test/logger.test.js"
      to: "src/utils/logger.js"
      via: "imports logger"
      pattern: "require.*src/utils/logger"
---

<objective>
Write comprehensive unit tests for the hierarchical configuration loader, validation logic, and logger using Node.js built-in test runner (`node:test` and `node:assert`).

Purpose: Ensure robust configuration loading and validation across all environments without regression.
Output: test/config.test.js, test/logger.test.js.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architecture-multi-source-configuration/01-01-PLAN.md
@src/config/index.js
@src/config/defaults.js
@src/utils/logger.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement configuration loader and validator unit tests</name>
  <files>test/config.test.js</files>
  <read_first>src/config/index.js, src/config/defaults.js</read_first>
  <action>
    Create `test/config.test.js` using `node:test` and `node:assert/strict`:
    - Test 1: Load default configuration when no file or env vars exist
    - Test 2: Load and merge JSON configuration from custom file path
    - Test 3: Environment variable overrides take precedence over file and default configs
    - Test 4: Numeric parsing and coercion for latitudes, longitudes, altitudes, and cooldowns
    - Test 5: Validation passes on valid config object
    - Test 6: Validation throws descriptive error when geofence bounds are inverted (e.g. minLat > maxLat)
    - Test 7: Validation throws descriptive error when required paths or broker URLs are empty
  </action>
  <verify>node --test test/config.test.js</verify>
  <done>All config unit tests execute and pass</done>
</task>

<task type="auto">
  <name>Task 2: Implement logger unit tests</name>
  <files>test/logger.test.js</files>
  <read_first>src/utils/logger.js</read_first>
  <action>
    Create `test/logger.test.js` using `node:test` and `node:assert/strict`:
    - Test 1: Logger respects `LOG_LEVEL` filtering (e.g. debug messages suppressed when level is 'info')
    - Test 2: Log messages contain timestamp and level labels
    - Test 3: `setLevel` dynamically adjusts active log level threshold
  </action>
  <verify>node --test test/logger.test.js</verify>
  <done>All logger unit tests execute and pass</done>
</task>

<task type="auto">
  <name>Task 3: Verify overall test suite execution</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
    Run `npm test` to confirm that the full test runner discovers and executes all tests in the `test/` directory cleanly without errors or unhandled rejections.
  </action>
  <verify>npm test</verify>
  <done>npm test runs all unit tests and exits with code 0</done>
</task>
</tasks>

<verification>
- [ ] test/config.test.js covers default config, custom JSON config, env overrides, and validation failure modes
- [ ] test/logger.test.js covers log level filtering and methods
- [ ] npm test runs and all tests pass
</verification>

<success_criteria>
- All unit tests pass cleanly
- High test coverage for configuration and validation logic
- No external test framework dependencies required
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-multi-source-configuration/01-02-SUMMARY.md`
</output>
