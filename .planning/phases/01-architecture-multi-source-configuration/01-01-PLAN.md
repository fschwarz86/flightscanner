---
phase: "01"
plan: "01"
title: "Initialize package.json, project structure, logger, and hierarchical config loader"
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/utils/logger.js
  - src/config/defaults.js
  - src/config/index.js
autonomous: true
requirements:
  - CONF-01
  - CONF-03
must_haves:
  truths:
    - "Project has package.json with dependencies and npm test script"
    - "Configuration resolves hierarchically from /etc/flightscanner/config.json, /etc/default/flightscanner, .env, and defaults"
    - "Coordinates bounding box, altitude limits, and MQTT connection settings are configurable without code edits"
  artifacts:
    - path: "package.json"
      provides: "Node.js dependencies and lifecycle scripts"
      contains: "\"name\": \"flightscanner\""
    - path: "src/utils/logger.js"
      provides: "Structured logging with level filtering and timestamping"
      exports: ["logger"]
    - path: "src/config/defaults.js"
      provides: "Default bounding box coordinates, altitude limits, and paths"
      exports: ["defaultConfig"]
    - path: "src/config/index.js"
      provides: "Hierarchical configuration loader and validator"
      exports: ["loadConfig", "validateConfig"]
  key_links:
    - from: "src/config/index.js"
      to: "src/config/defaults.js"
      via: "merges loaded file/env configs over defaultConfig"
      pattern: "require.*defaults"
    - from: "src/config/index.js"
      to: "src/utils/logger.js"
      via: "logs configuration loading source and warnings"
      pattern: "logger"
---

<objective>
Initialize the package structure and implement the hierarchical configuration loader and structured logger for flightscanner.

Purpose: Provide a solid foundation for modular services and allow configuration to be loaded from /etc/flightscanner/config.json, /etc/default/flightscanner, local .env, or process.env without hardcoded secrets or hardcoded coordinates.
Output: package.json, .env.example, src/utils/logger.js, src/config/defaults.js, src/config/index.js.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@flightscanner.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create package.json and project directory layout</name>
  <files>package.json, .env.example</files>
  <read_first>flightscanner.js, .planning/codebase/STACK.md</read_first>
  <action>
    1. Create `package.json` with:
       - `name`: "flightscanner", `version`: "1.0.0", `description`: "ADS-B flight tracking and Awtrix LED matrix notification service"
       - `main`: "src/index.js"
       - `scripts`: { "start": "node src/index.js", "test": "node --test test/**/*.test.js" }
       - `engines`: { "node": ">=18.0.0" }
       - `dependencies`: { "axios": "^1.7.9", "chokidar": "^3.6.0", "dotenv": "^16.4.7", "flightradarapi": "^1.0.8", "mqtt": "^5.10.3", "papaparse": "^5.5.2" }
    2. Create `.env.example` with documented environment variable keys:
       - DUMP1090_FILE_PATH, AIRCRAFT_TYPES_CSV_PATH
       - MQTT_BROKER_URL, MQTT_USERNAME, MQTT_PASSWORD, MQTT_TOPIC
       - GEOFENCE_MIN_LAT, GEOFENCE_MAX_LAT, GEOFENCE_MIN_LON, GEOFENCE_MAX_LON
       - ALTITUDE_MIN_FT, ALTITUDE_MAX_FT, FLIGHT_COOLDOWN_MS, LOG_LEVEL
  </action>
  <verify>test -f package.json &amp;&amp; test -f .env.example &amp;&amp; node -e 'require("./package.json")'</verify>
  <done>package.json and .env.example are created and valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Implement structured logger</name>
  <files>src/utils/logger.js</files>
  <read_first>.planning/codebase/CONVENTIONS.md</read_first>
  <action>
    Create `src/utils/logger.js` with structured console logging:
    - Support log levels: `debug`, `info`, `warn`, `error`
    - Include ISO timestamp, log level label, and formatted message/metadata
    - Filter messages based on configured `LOG_LEVEL` (default: 'info')
    - Format output cleanly for systemd / journalctl compatibility
    - Export `logger` object with `debug`, `info`, `warn`, `error`, and `setLevel` functions
  </action>
  <verify>node -e 'const { logger } = require("./src/utils/logger"); logger.info("Logger initialized");'</verify>
  <done>src/utils/logger.js exports functioning logger methods</done>
</task>

<task type="auto">
  <name>Task 3: Implement hierarchical configuration loader and validator</name>
  <files>src/config/defaults.js, src/config/index.js</files>
  <read_first>flightscanner.js, .planning/codebase/INTEGRATIONS.md</read_first>
  <action>
    1. Create `src/config/defaults.js` defining default configuration:
       - `dump1090FilePath`: '/run/dump1090-fa/aircraft.json'
       - `aircraftTypesCsvPath`: '/etc/flightdata/aircraft_types.csv'
       - `mqtt`: { `brokerUrl`: 'mqtt://homeassistant:1883', `username`: '', `password`: '', `topic`: 'awtrix/cmd/notify', `clientId`: 'flightscanner' }
       - `geofence`: { `minLat`: 53.65, `maxLat`: 53.72, `minLon`: 10.10, `maxLon`: 10.20 }
       - `altitude`: { `minFt`: 2000, `maxFt`: 10000 }
       - `cooldownMs`: 900000 (15 minutes)
       - `cacheTtlMs`: 14400000 (4 hours)
       - `logLevel`: 'info'
    2. Create `src/config/index.js` with `loadConfig(customConfigPath)` and `validateConfig(config)`:
       - Check for config file in order:
         a. Explicit `customConfigPath` or `process.env.CONFIG_FILE`
         b. `/etc/flightscanner/config.json`
         c. `/etc/default/flightscanner` (parsed as KEY=VALUE env-file)
         d. Local `.env` (via dotenv)
       - Merge hierarchy: `defaults` &lt;- `file config` &lt;- `environment variables`
       - Convert and validate numbers for geofence/altitude/cooldown
       - Validate that required fields (dump1090FilePath, mqtt.brokerUrl, geofence bounds) are present and valid
       - Log config loading source (without exposing passwords)
  </action>
  <verify>node -e 'const { loadConfig, validateConfig } = require("./src/config"); const cfg = loadConfig(); validateConfig(cfg); console.log("Config valid, broker:", cfg.mqtt.brokerUrl);'</verify>
  <done>src/config/index.js and src/config/defaults.js properly resolve and validate configuration</done>
</task>
</tasks>

<verification>
- [ ] package.json exists and is valid
- [ ] .env.example documents all available configuration options
- [ ] src/utils/logger.js provides timestamped logging for journalctl
- [ ] src/config/index.js successfully resolves configuration from defaults, files, and environment variables
</verification>

<success_criteria>
- All tasks completed
- Configuration loader supports /etc/flightscanner/config.json, .env, and environment overrides
- No hardcoded secrets in source code
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-multi-source-configuration/01-01-SUMMARY.md`
</output>
