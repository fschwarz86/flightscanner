---
quick_id: 260907-cb4
slug: make-short-notification-airport-code-con
title: "Make short notification airport code configurable and match strictly by airport code (excluding XFW)"
date: "2026-09-07"
files_modified:
  - src/config/defaults.js
  - src/config/index.js
  - config/flightscanner.json.example
  - config/default-flightscanner.example
  - src/services/notification/formatter.js
  - src/services/notification/mqtt.js
  - test/config.test.js
  - test/formatter.test.js
autonomous: true
---

<objective>
Configure the base airport code for short notifications (defaulting to "HAM"), and restrict short-form matching strictly to the configured airport code (e.g. `(${baseAirport})` or `${baseAirport}`) so other airports with "Hamburg" in their name/city (specifically Hamburg Finkenwerder / XFW) are not mistakenly matched or shortened.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add baseAirport to configuration, defaults, env mapping, and examples</name>
  <files>src/config/defaults.js, src/config/index.js, config/flightscanner.json.example, config/default-flightscanner.example, test/config.test.js</files>
  <action>
    - In `src/config/defaults.js`: add `baseAirport: "HAM"`.
    - In `src/config/index.js`:
      - Map `BASE_AIRPORT` and `HOME_AIRPORT` in `applyEnvMapToConfig`.
      - Validate `baseAirport` in `validateConfig`.
    - In `config/flightscanner.json.example` and `config/default-flightscanner.example`: add `baseAirport: "HAM"` / `BASE_AIRPORT=HAM`.
    - In `test/config.test.js`: add tests for default, env override, and invalid `baseAirport`.
  </action>
  <verify>npm test test/config.test.js</verify>
  <done>Config correctly exposes, merges, and validates baseAirport</done>
</task>

<task type="auto">
  <name>Task 2: Restrict short notification matching to exact airport code and support configurable base airport</name>
  <files>src/services/notification/formatter.js, src/services/notification/mqtt.js, test/formatter.test.js</files>
  <action>
    - In `src/services/notification/formatter.js`:
      - Add `"XFW": "Hamburg-Finkenwerder"` to `AIRPORT_CITIES`.
      - Add `isBaseAirport(formattedAirport, baseAirportCode)` helper that checks for `(${baseAirportCode})` or exact `${baseAirportCode}`.
      - Remove loose `startsWith("hamburg")` checks so XFW (or other airports) never trigger short format.
      - Support configurable `baseAirport` in `formatNotificationText(flightData, options)` and `formatNotificationPayload(flightData, options)`.
    - In `src/services/notification/mqtt.js`:
      - Pass `config.baseAirport` into `formatNotificationPayload`.
    - In `test/formatter.test.js`:
      - Add test verifying XFW as destination does NOT omit destination (`-> Hamburg-Finkenwerder (XFW)`).
      - Add test verifying XFW as origin does NOT omit origin (`Hamburg-Finkenwerder (XFW) ->`).
      - Add test for custom `baseAirport` (e.g. "BER" or "MUC") correctly shortening arrivals and departures for that airport and not HAM.
  </action>
  <verify>npm test</verify>
  <done>All tests pass and XFW is never shortened as HAM</done>
</task>
</tasks>

<verification>
- `npm test` runs with 0 failures across all test suites
- XFW flights retain their full origin/destination
- HAM flights are shortened according to direction (`->` or `<-`)
- Configurable base airport functions as expected when set to another code
</verification>
