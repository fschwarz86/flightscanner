---
quick_id: 260907-c1u
slug: change-the-notification-for-incoming-fli
title: "Omit destination airport if HAM and show <- $ORIGIN for incoming flights"
date: "2026-09-07"
files_modified:
  - src/services/notification/formatter.js
  - test/formatter.test.js
autonomous: true
---

<objective>
Change flight notification formatting in `src/services/notification/formatter.js` for incoming flights so that if the destination airport is Hamburg (HAM), the destination is omitted and the format displays `<- $ORIGIN` (symmetrical to outgoing flights displaying `-> $DESTINATION`). Update and extend tests in `test/formatter.test.js` to verify incoming, outgoing, transit, and edge cases.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update formatNotificationText for incoming Hamburg flights</name>
  <files>src/services/notification/formatter.js</files>
  <action>
    In `src/services/notification/formatter.js`:
    - Add detection for Hamburg destination `isHamburgDestination` (checking if destination contains "(HAM)" or starts with "hamburg", case-insensitive).
    - If `isHamburgDestination` is true and `!isHamburgOrigin`:
      Return `${flightLabel}${aircraftTag} <- ${origin || "Unbekannt"}`.
    - If `isHamburgOrigin` is true and `!isHamburgDestination`:
      Return `${flightLabel}${aircraftTag} -> ${destination || "Unbekannt"}`.
    - If both or neither are Hamburg, preserve existing multi-airport `${origin} -> ${destination}` logic.
  </action>
  <verify>node -e 'const { formatNotificationText } = require("./src/services/notification/formatter"); console.log(formatNotificationText({ airline: "Lufthansa", callsign: "DLH123", origin: "MUC", destination: "Hamburg (HAM)", aircraft: "Airbus A320" }));'</verify>
  <done>formatNotificationText outputs `Lufthansa Flug DLH123 (Airbus A320) <- München (MUC)` for HAM arrivals</done>
</task>

<task type="auto">
  <name>Task 2: Update unit test suite in test/formatter.test.js</name>
  <files>test/formatter.test.js</files>
  <action>
    In `test/formatter.test.js`:
    - Update Hamburg arrival tests to expect `<- München (MUC)`.
    - Add test for overflight / transit where neither airport is Hamburg (`München (MUC) -> Stockholm (ARN)`).
    - Update `formatNotificationPayload` test assertion to match `<- München (MUC)`.
  </action>
  <verify>npm test</verify>
  <done>All tests in test suite pass cleanly</done>
</task>
</tasks>

<verification>
- `npm test` runs all test suites with 0 failures
- Verification of incoming Hamburg flight format: `<- $ORIGIN`
- Verification of outgoing Hamburg flight format: `-> $DESTINATION`
- Verification of non-Hamburg route format: `$ORIGIN -> $DESTINATION`
</verification>
