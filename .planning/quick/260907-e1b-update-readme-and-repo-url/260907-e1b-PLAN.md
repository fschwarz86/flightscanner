---
task_id: 260907-e1b
slug: update-readme-and-repo-url
title: "Update README with latest changes and fix repo URL to fschwarz86/flightscanner"
date: "2026-09-07"
status: in-progress
---

# Quick Task Plan: 260907-e1b

Update README.md with the latest project features (short notifications with base airport configuration, rate-adaptive display repetitions, 79 automated tests) and update repository URLs to `https://github.com/fschwarz86/flightscanner`.

## Planned Tasks

1. **Update Repository URLs**:
   - In `README.md`: update `https://github.com/fschwarz/flightscanner.git` to `https://github.com/fschwarz86/flightscanner.git`.
   - In `systemd/flightscanner.service`: update `Documentation=https://github.com/fschwarz/flightscanner` to `https://github.com/fschwarz86/flightscanner`.

2. **Update README.md with Recent Capabilities**:
   - Update test suite count from 47 to 79 tests.
   - Document `BASE_AIRPORT` / `baseAirport` configuration (default `HAM`) and short format for arrivals (`<- $ORIGIN`) and departures (`-> $DESTINATION`), including strict code matching (distinguishing HAM from XFW).
   - Document `DISPLAY_REPEAT` / `repeat` / `displayRepeat` configuration (default `2`) and automatic reduction to `1` repetition when sending >1 message per minute.
   - Update configuration parameters table with `BASE_AIRPORT` and `DISPLAY_REPEAT`.
   - Update `.env.example` to include `BASE_AIRPORT=HAM` and `DISPLAY_REPEAT=2`.

3. **Verify and Commit**:
   - Run tests (`npm test`) to ensure everything is clean.
   - Commit changes, create SUMMARY.md, and update STATE.md.
